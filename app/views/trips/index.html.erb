<div class="standard_box">
  <% if @trips.count != 0 %>
    <% @trips.each do |trip| %>
      <div class="trips_list">
        <h3><%= link_to trip.origin.city, trips_path(id: trip.id) %><h3>
        <h4>From <%= trip.start_at %> to <%= trip.end_at %></h4>
      </div>
    <% end %>
  <% else %>
    <h1> You don't have any Trips </h1>
  <% end %>
</div>
